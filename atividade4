CREATE TABLE CLIENTE (
  ID INT AUTO_INCREMENT PRIMARY KEY,
  NOME_COMPLETO VARCHAR(255) NOT NULL,
  CIDADE VARCHAR(255) NOT NULL,
  ESTADO CHAR(2) NOT NULL
);

CREATE TABLE PEDIDO(
  ID INT AUTO_INCREMENT PRIMARY KEY,
  CLIENTE_ID INT,
  DATA_PEDIDO DATETIME DEFAULT CURRENT_TIMESTAMP,
  VALOR DECIMAL(10,2) NOT NULL,
  FOREIGN KEY (CLIENTE_ID) REFERENCES CLIENTE(ID)
);

INSERT INTO CLIENTE (NOME_COMPLETO , CIDADE, ESTADO) VALUES
("Fernanda Alves", "Londrina", "PR"),
("Gabriel Santos", "Cascavel", "PR"),
("Helena Martins", "Campinas", "SP"),
("Igor Pereira", "Foz do Iguaçu", "PR"),
("Juliana Mendes", "Joinville", "SC");

INSERT INTO PEDIDO (CLIENTE_ID, VALOR) VALUES
(1,  210.00),
(1,  115.50),
(2,  450.00),
(2,  80.00),
(3,  129.90),
(3,  520.00),
(4,  610.00),
(4,  95.00),
(5,  340.00),
(5,  230.00);

CREATE VIEW vw_total_cliente AS
SELECT
    c.nome_completo AS nome_cliente,
    COUNT(p.id) AS quantidade_pedidos,
    SUM(p.valor) AS valor_total_gasto
FROM cliente c
JOIN pedido p ON p.cliente_id = c.id
GROUP BY c.id, c.nome_completo;

SELECT * FROM vw_total_cliente;
